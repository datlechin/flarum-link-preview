export declare function parseList(value: string): string[];
export default function matchesList(url: string, entries: readonly string[]): boolean;
export declare function allowsUrl(url: string): boolean;
