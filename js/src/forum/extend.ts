// Nothing is extended declaratively yet. Core's `bootExtensions` skips an
// extension whose `extend` export is missing, so this is not required; it is
// kept as the one place a future IExtender would go.
export default [];
